import React from "react";

function Monday({schedule}){
    // const mondaySchedule = schedule.map(data =><li>{data.activities}</li>)
    console.log(schedule)

return(
    <div>
        hi
    </div>
)}

export default Monday;